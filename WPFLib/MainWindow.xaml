<Window x:Class="WPFLib.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFLib"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        Style="{DynamicResource WindowStyle}">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="Name" Text="TextBlock 1" FontSize="18" Style="{DynamicResource TextBlockStyle}"/>
            <TextBlock x:Name="Age" Text="TextBlock 2" FontSize="18" Style="{DynamicResource TextBlockStyle2}"/>
            <TextBox x:Name="TextBox" Width="200" Height="30" Style="{DynamicResource TextBoxStyle}"/>
            <Button x:Name="Btn" Content="Send" Width="100" Height="30" Click="Btn_Click" Style="{DynamicResource ButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
